Збілдити образ

```
docker compose build
```

Запустити міграції

```
docker compose run --rm migrate
```

Запустити seed

```
docker compose run --rm seed
```

Запустити app

```
docker compose up -d
```